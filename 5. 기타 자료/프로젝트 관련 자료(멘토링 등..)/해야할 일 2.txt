1. 상품 구매 시 리뷰 작성 권한

2. 이미지 처리 부분 알려주기
3. 빨리 다 끝내고 게시판쪽 만들어보기


사용자
   │
   │ 리뷰 내용 입력 + 이미지 선택
   ▼
FE (프론트엔드)
   │
   │ POST /api/images/reviews (MultipartFile[] 전송) : form date형식
   │──────────────────────────────►
   │                                서버
   │                                │
   │                                │ 이미지 저장 후 URL 반환
   │                                └──────────────► imageUrls[]
   │
   │ GET /api/products/{product_id}/reviews/{user_id}/exists : json형식
   │──────────────────────────────► 서버
   │                                │
   │                                │ DB 조회
   │                                └──────────────► { hasReview: true/false }
   │
   │ hasReview 판단
   ├─ true  → POST /api/products/{product_id} (리뷰만 저장) : json형식
   │              │────────────────────────► 서버
   │              │                         │ 리뷰 저장
   │              │                         └───────────► ProductReviewDTO
   └─ false → POST /api/reviews/{product_id} (리뷰 + 포인트) : json형식
                  │────────────────────────► 서버
                  │                         │ 리뷰 + 포인트 적립
                  │                         └───────────► CreateReviewResponseDTO
	          │
	          ▼
		  FE
	          │
	          │ 리뷰 목록 갱신 / UI 업데이트
	          │ 포인트 적립 UI 업데이트 (첫 리뷰인 경우)
	          ▼
	       사용자
	          │
	          │ 
		완료




OrderItemDTO
product_price뭐임? 그냥 price인가?

OrderSaveDTO에서 왜 order_id long?
QnAListDTO에서 qna_id 왜 long?


ReviewDTO에서 image_url왜 단일 url인지

UserMapper에서 findBannedById는 왜 혼자 int? user_id인가?